# Invite Verifier (Public)

Place invite codes in `codes.json` under the key "valid_codes".  
Access at: https://<username>.github.io/<repo>/codes.json

App can fetch this JSON URL and check the code client-side.